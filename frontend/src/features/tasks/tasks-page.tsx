import { Filter, MoreHorizontal, Plus, Search, SlidersHorizontal } from 'lucide-react'
import { Link } from 'react-router-dom'
import { useMemo, useState } from 'react'
import { Button } from '@/components/ui/button'

const initialTasks = [
  { id: '1', title: 'Polish workspace onboarding', description: 'Tighten the visual flow and empty states.', status: 'In progress', priority: 'High', tag: 'Design', assignee: 'AM', due: 'Today' },
  { id: '2', title: 'Review API access controls', description: 'Confirm workspace role permissions.', status: 'To do', priority: 'High', tag: 'Engineering', assignee: 'NP', due: 'Jul 16' },
  { id: '3', title: 'Prepare customer QBR notes', description: 'Capture wins, risks and next opportunities.', status: 'In progress', priority: 'Medium', tag: 'Customer', assignee: 'SL', due: 'Jul 18' },
  { id: '4', title: 'Audit reporting dashboard', description: 'Validate weekly metric definitions.', status: 'Done', priority: 'Low', tag: 'Analytics', assignee: 'AM', due: 'Jul 12' },
  { id: '5', title: 'Plan August product launch', description: 'Map campaign deliverables and owners.', status: 'To do', priority: 'Medium', tag: 'Marketing', assignee: 'JK', due: 'Jul 22' },
]
const statusStyle: Record<string, string> = { 'To do': 'bg-slate-100 text-slate-600', 'In progress': 'bg-blue-50 text-blue-700', Done: 'bg-emerald-50 text-emerald-700' }

export function TasksPage() {
  const [query, setQuery] = useState(''); const [filter, setFilter] = useState('All')
  const tasks = useMemo(() => initialTasks.filter((task) => (filter === 'All' || task.status === filter) && task.title.toLowerCase().includes(query.toLowerCase())), [query, filter])
  return <div className="mx-auto max-w-7xl"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-sm font-semibold text-blue-600">WORK MANAGEMENT</p><h2 className="heading mt-1 text-3xl font-extrabold tracking-tight">My tasks</h2><p className="mt-2 text-sm text-slate-500">Stay focused on what matters most.</p></div><Link to="/tasks/new"><Button><Plus size={17}/> New task</Button></Link></div><div className="surface mt-7 overflow-hidden"><div className="flex flex-col gap-3 border-b border-slate-100 p-4 sm:flex-row sm:items-center sm:justify-between"><div className="relative max-w-sm flex-1"><Search size={17} className="absolute left-3 top-1/2 -translate-y-1/2 text-slate-400"/><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search tasks..." className="h-10 w-full rounded-xl border border-slate-200 bg-slate-50 pl-10 pr-3 text-sm outline-none focus:border-blue-500 focus:ring-4 focus:ring-blue-500/10"/></div><div className="flex items-center gap-2"><Filter size={16} className="text-slate-400"/>{['All', 'To do', 'In progress', 'Done'].map((option) => <button onClick={() => setFilter(option)} key={option} className={`rounded-lg px-3 py-2 text-xs font-bold transition ${filter === option ? 'bg-slate-900 text-white' : 'text-slate-500 hover:bg-slate-100'}`}>{option}</button>)}<button className="grid h-9 w-9 place-items-center rounded-lg text-slate-500 hover:bg-slate-100"><SlidersHorizontal size={17}/></button></div></div><div className="hidden grid-cols-[minmax(260px,1.8fr)_120px_120px_110px_100px_45px] gap-4 border-b border-slate-100 bg-slate-50/70 px-6 py-3 text-xs font-bold uppercase tracking-wider text-slate-400 md:grid"><span>Task</span><span>Status</span><span>Priority</span><span>Assignee</span><span>Due</span></div>{tasks.length ? <div className="divide-y divide-slate-100">{tasks.map((task) => <div key={task.id} className="grid gap-3 p-5 md:grid-cols-[minmax(260px,1.8fr)_120px_120px_110px_100px_45px] md:items-center md:gap-4 md:px-6"><div className="flex gap-3"><button className="mt-1 h-5 w-5 shrink-0 rounded-full border-2 border-slate-300 hover:border-blue-500" aria-label={`Complete ${task.title}`}/><div className="min-w-0"><Link to={`/tasks/${task.id}/edit`} className="block truncate text-sm font-bold hover:text-blue-600">{task.title}</Link><p className="mt-1 truncate text-xs text-slate-500">{task.description}</p></div></div><span className={`w-fit rounded-lg px-2.5 py-1 text-xs font-bold ${statusStyle[task.status]}`}>{task.status}</span><span className={`text-xs font-bold ${task.priority === 'High' ? 'text-red-500' : task.priority === 'Medium' ? 'text-amber-500' : 'text-slate-400'}`}>{task.priority}</span><span className="grid h-7 w-7 place-items-center rounded-full bg-blue-100 text-[10px] font-bold text-blue-700">{task.assignee}</span><span className="text-xs font-semibold text-slate-500">{task.due}</span><button className="text-slate-400 hover:text-slate-800"><MoreHorizontal size={18}/></button></div>)}</div> : <div className="grid min-h-72 place-items-center p-8 text-center"><div><span className="mx-auto grid h-12 w-12 place-items-center rounded-2xl bg-slate-100 text-slate-400"><Search size={20}/></span><p className="mt-4 font-bold">No tasks found</p><p className="mt-1 text-sm text-slate-500">Try a different search or filter.</p></div></div>}<div className="flex items-center justify-between border-t border-slate-100 p-4 text-xs text-slate-500"><span>Showing {tasks.length} of {initialTasks.length} tasks</span><div className="flex gap-2"><button className="rounded-lg border px-3 py-1.5 disabled:opacity-40" disabled>Previous</button><button className="rounded-lg border px-3 py-1.5">Next</button></div></div></div></div>
}
