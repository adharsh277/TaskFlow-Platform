import { Filter, MoreHorizontal, Plus, Search } from 'lucide-react'
import { Link } from 'react-router-dom'
import { useMemo, useState } from 'react'
import toast from 'react-hot-toast'
import { motion } from 'framer-motion'
import { Button } from '@/components/ui/button'
import { statusClass, type TaskStatus, useTasks } from './task-store'

const statuses: Array<'All' | TaskStatus> = ['All', 'Pending', 'In Progress', 'Completed', 'Dropped']
export function TasksPage() { const { tasks, setStatus, deleteTask } = useTasks(); const [query, setQuery] = useState(''); const [status, setStatusFilter] = useState<'All' | TaskStatus>('All'); const [priority, setPriority] = useState('All'); const [due, setDue] = useState('All'); const [open, setOpen] = useState<string | null>(null)
  const filtered = useMemo(() => tasks.filter((task) => { const term = query.toLowerCase(); const search = !term || `${task.title} ${task.description} ${task.category}`.toLowerCase().includes(term); const date = due === 'All' || (due === 'Today' && new Date(task.dueDate).toDateString() === new Date().toDateString()) || (due === 'Upcoming' && new Date(task.dueDate) > new Date()); return search && (status === 'All' || task.status === status) && (priority === 'All' || task.priority === priority) && date }), [tasks, query, status, priority, due])
  const action = (id: string, next: TaskStatus) => { setStatus(id, next); toast.success(`Task marked as ${next}`); setOpen(null) }
  return <div className="mx-auto max-w-7xl"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-sm font-semibold text-blue-600">WORK MANAGEMENT</p><h2 className="heading mt-1 text-3xl font-extrabold tracking-tight">My tasks</h2><p className="mt-2 text-sm text-slate-500">Stay focused on what matters most.</p></div><Link to="/tasks/new"><Button><Plus size={17}/> New task</Button></Link></div><div className="surface mt-7 overflow-visible"><div className="space-y-3 border-b border-slate-100 p-4"><div className="relative max-w-md"><Search size={17} className="absolute left-3 top-1/2 -translate-y-1/2 text-slate-400"/><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search title, description or category..." className="h-10 w-full rounded-xl border border-slate-200 bg-slate-50 pl-10 pr-3 text-sm outline-none focus:border-blue-500 focus:ring-4 focus:ring-blue-500/10"/></div><div className="flex flex-wrap items-center gap-2"><Filter size={16} className="text-slate-400"/>{statuses.map((item) => <button onClick={() => setStatusFilter(item)} key={item} className={`rounded-lg px-3 py-2 text-xs font-bold transition ${status === item ? 'bg-slate-900 text-white' : 'text-slate-500 hover:bg-slate-100'}`}>{item}</button>)}<select value={priority} onChange={(event) => setPriority(event.target.value)} className="h-9 rounded-lg border bg-white px-2 text-xs font-bold text-slate-600"><option>All</option><option>Low</option><option>Medium</option><option>High</option></select><select value={due} onChange={(event) => setDue(event.target.value)} className="h-9 rounded-lg border bg-white px-2 text-xs font-bold text-slate-600"><option>All</option><option>Today</option><option>Upcoming</option></select></div></div>{filtered.length ? <div className="grid gap-4 p-4 sm:grid-cols-2 xl:grid-cols-3">{filtered.map((task) => <motion.article key={task.id} layout whileHover={{ y: -3 }} className="relative rounded-xl border border-slate-200 bg-white p-4 shadow-sm transition-shadow hover:shadow-soft"><div className="flex items-start justify-between gap-2"><span className={`rounded-lg px-2.5 py-1 text-xs font-bold ${statusClass[task.status]}`}>{task.status}</span><div className="relative"><button onClick={() => setOpen(open === task.id ? null : task.id)} className="rounded-lg p-1 text-slate-400 hover:bg-slate-100 hover:text-slate-700" aria-label={`Actions for ${task.title}`}><MoreHorizontal size={19}/></button>{open === task.id && <div className="absolute right-0 z-20 mt-1 w-48 rounded-xl border bg-white p-1.5 shadow-floating"><Link to={`/tasks/${task.id}/edit`} className="block rounded-lg px-3 py-2 text-sm font-semibold hover:bg-slate-50">Edit</Link><button onClick={() => action(task.id, 'Completed')} className="w-full rounded-lg px-3 py-2 text-left text-sm font-semibold hover:bg-slate-50">Mark as Completed</button><button onClick={() => action(task.id, 'In Progress')} className="w-full rounded-lg px-3 py-2 text-left text-sm font-semibold hover:bg-slate-50">Mark as In Progress</button><button onClick={() => action(task.id, 'Dropped')} className="w-full rounded-lg px-3 py-2 text-left text-sm font-semibold hover:bg-slate-50">Mark as Dropped</button><button onClick={() => { deleteTask(task.id); toast.success('Task deleted'); setOpen(null) }} className="w-full rounded-lg px-3 py-2 text-left text-sm font-semibold text-red-600 hover:bg-red-50">Delete</button></div>}</div></div><Link to={`/tasks/${task.id}/edit`} className="mt-4 block text-sm font-extrabold hover:text-blue-600">{task.title}</Link><p className="mt-2 line-clamp-2 min-h-10 text-xs leading-5 text-slate-500">{task.description}</p><div className="mt-5 flex items-center justify-between border-t border-slate-100 pt-3 text-xs"><span className="font-bold text-slate-500">{task.category} · {task.priority}</span><span className="text-slate-400">Due {new Date(task.dueDate).toLocaleDateString(undefined, { month: 'short', day: 'numeric' })}</span></div></motion.article>)}</div> : <div className="grid min-h-72 place-items-center p-8 text-center"><div><span className="mx-auto grid h-12 w-12 place-items-center rounded-2xl bg-blue-50 text-blue-600"><Search size={20}/></span><p className="mt-4 font-bold">{tasks.length ? 'No matching tasks' : 'No tasks yet.'}</p><p className="mt-1 text-sm text-slate-500">{tasks.length ? 'Try adjusting your search or filters.' : 'Create your first task and build momentum.'}</p>{!tasks.length && <Link to="/tasks/new"><Button className="mt-5">Create Your First Task</Button></Link>}</div></div>}<div className="border-t border-slate-100 p-4 text-xs text-slate-500">Showing {filtered.length} of {tasks.length} tasks</div></div></div> }
